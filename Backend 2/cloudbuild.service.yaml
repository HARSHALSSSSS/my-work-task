steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file
      - ${_DOCKERFILE}
      - --tag
      - gcr.io/$PROJECT_ID/${_IMAGE_NAME}
      - .

images:
  - gcr.io/$PROJECT_ID/${_IMAGE_NAME}
